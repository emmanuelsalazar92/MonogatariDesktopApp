# Server-only Notion internal integration token. Never expose this as NEXT_PUBLIC_*.
NOTION_API_TOKEN=

# Optional, comma-separated public origins for a configured reverse proxy or
# hostname. LAN IPs and localhost work automatically and should not be listed.
# MONOGATARI_TRUSTED_ORIGINS=https://monogatari.example.test
